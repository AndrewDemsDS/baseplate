## What this changes

A short description and the reason for it.

## Checklist

- [ ] `docker compose config` and the per-profile configs parse.
- [ ] `yamllint` and `shellcheck` pass.
- [ ] No personal data in the diff (domains, IPs, emails, tokens, hashes).
- [ ] New services are behind a `profiles:` entry; stateful ones are pinned and
      excluded from Watchtower.
- [ ] Docs updated (README / .env.example / CHANGELOG) if behavior changed.

## AI assistance

- [ ] AI tooling helped with this change (commit carries an `Assisted-by: AI` trailer).
